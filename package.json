{
    "scripts": {
        "start": "elm-live Main.elm --output=public/app.js --pushstate --dir=public/ --open",
        "broadcast": "elm-live Main.elm --output=public/app.js --pushstate --dir=public/ --host:0.0.0.0"
    }
}